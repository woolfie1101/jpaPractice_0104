spring:
  #springboot 에서는 알아서 메모리 DB로 테스트를 해준다. 그러므로 아래 설정이 필요없다.

#  datasource:
#    url: jdbc:h2:mem:test
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
#
#  jpa:
#    hibernate:
#      ddl-auto: create-drop #자동으로 테이블 지우고 다시 생성하고 초기화
#    properties:
#      hibernate:
#        #        show_sql: true #sout 를 통해서 sql 을 보여준다
#        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug #로거를 통해서 보여준다.
    org.hibernate.type: trace